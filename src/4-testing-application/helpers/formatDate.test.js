/**
 * 👉 TASK: Testing utilities
 *
 * It sometimes important to test utility functions as well,
 * especially if they are complex or heavily used
 *
 * Let's practice by testing `formatDate` function
 */

import formatDate from "./formatDate";

test.todo("formats date relatively if less than day ago");

test.todo("returns formatted date fallback if more than day ago");
