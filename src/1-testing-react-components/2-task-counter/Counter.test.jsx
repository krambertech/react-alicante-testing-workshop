//import { render, screen } from "@testing-library/react";
//import userEvent from "@testing-library/user-event";

//import Counter from "./Counter";

test.todo("renders with initial value 0 and increment and decrement buttons");

test.todo("allows increment and decrement");

// 🚀 Bonus task: Does not allow to go below min and above max
// test.todo("does not allow to go below min and above max");

// 🚀  Bonus task: (TDD) Displays alert if count becomes out of boundaries
// test.todo("displays alert if count becomes out of boundaries");
